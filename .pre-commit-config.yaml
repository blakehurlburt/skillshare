repos:
  - repo: local
    hooks:
      - id: repository-tests
        name: repository tests
        entry: python3 -m unittest discover -s tests -v
        language: system
        pass_filenames: false
        always_run: true

